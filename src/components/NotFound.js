import React from 'react';
import '../style.css'
// Display No results found in conjunction with Search component

const NotFound = () => (
    <ul className="not-found">

    </ul>
);

export default NotFound;
